apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: virtual-closet-staging
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
