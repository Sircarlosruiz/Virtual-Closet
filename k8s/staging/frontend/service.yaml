apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: virtual-closet-staging
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
