services:
  postgres:
    image: postgres:16-alpine
    networks:
      - virtual_closet
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: virtual_closet_dev
    deploy:
      resources:
        limits:
          memory: 2gb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  frontend:
    networks:
      - virtual_closet
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: dev
    deploy:
      resources:
        limits:
          memory: 2gb
    command: pnpm dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
      NEXT_PUBLIC_BACKEND_URL: http://localhost:8000
      BACKEND_URL: http://fastapi:8000
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    depends_on:
      - fastapi

  fastapi:
    networks:
      - virtual_closet
    build:
      context: ./backend
      dockerfile: Dockerfile
    deploy:
      resources:
        limits:
          memory: 1.5gb
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
      - ./docs/imgs:/app/seed-imgs:ro
      - /app/.venv
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/virtual_closet_dev
      JWT_SECRET_KEY: dev-secret-key-change-in-production
      JWT_ALGORITHM: HS256
      JWT_EXPIRE_DAYS: "7"
      FRONTEND_URL: http://localhost:3000
      BACKEND_URL: http://localhost:8000
      MINIO_ENDPOINT: http://minio:9000
      MINIO_PUBLIC_ENDPOINT: http://localhost:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET_ORIGINALS: originals
      MINIO_BUCKET_GENERATED: generated
      MINIO_BUCKET_THUMBNAILS: thumbnails
      MINIO_BUCKET_MODEL_THUMBNAILS: model-thumbnails
      SEED_IMGS_DIR: /app/seed-imgs
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      CATVTON_LOCAL_URL: http://catvton:8000
      FASHN_LOCAL_URL: http://fashn:8000
      TRYOFF_MODEL_URL: http://tryoff-model:8000
      TRYOFF_MODEL_TIMEOUT_SECONDS: "1800"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  minio:
    image: minio/minio:latest
    deploy:
      resources:
        limits:
          memory: 1gb
    networks:
      - virtual_closet
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_API_CORS_ALLOW_ORIGIN: "*"
      MINIO_API_CORS_ALLOW_METHODS: "GET,PUT,POST,DELETE,HEAD,OPTIONS"
      MINIO_API_CORS_ALLOW_HEADERS: "*"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.12-management
    deploy:
      resources:
        limits:
          memory: 1gb
    networks:
      - virtual_closet
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5

  celery_worker:
    networks:
      - virtual_closet
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: uv run celery -A core.celery_app worker --loglevel=info -Q vton.generation.normal,vton.generation.priority,tryoff
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      - /app/.venv
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/virtual_closet_dev
      JWT_SECRET_KEY: dev-secret-key-change-in-production
      JWT_ALGORITHM: HS256
      JWT_EXPIRE_DAYS: "7"
      FRONTEND_URL: http://localhost:3000
      BACKEND_URL: http://localhost:8000
      MINIO_ENDPOINT: http://minio:9000
      MINIO_PUBLIC_ENDPOINT: http://localhost:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET_ORIGINALS: originals
      MINIO_BUCKET_GENERATED: generated
      MINIO_BUCKET_THUMBNAILS: thumbnails
      MINIO_BUCKET_MODEL_THUMBNAILS: model-thumbnails
      # VTON_PROVIDER, CATVTON_*, REPLICATE_API_KEY come from env_file (backend/.env)
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      # CatVTON-Flux self-hosted: worker calls http://catvton:8000/classify + /predict
      # Requires: docker compose --profile gpu up catvton (when VTON_PROVIDER=catvton_local)
      CATVTON_LOCAL_URL: http://catvton:8000
      # FASHN VTON v1.5 self-hosted: worker calls http://fashn:8000/classify + /predict
      # Requires: docker compose --profile gpu up fashn (when VTON_PROVIDER=fashn_local)
      FASHN_LOCAL_URL: http://fashn:8000
      # tryoff-model is profile gpu|tryoff — start: docker compose --profile tryoff up -d tryoff-model
      TRYOFF_MODEL_URL: http://tryoff-model:8000
      TRYOFF_MODEL_TIMEOUT_SECONDS: "1800"
    depends_on:
      rabbitmq:
        condition: service_healthy
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy

  # FASHN VTON v1.5 inference server — maskless try-on, ~8 GB VRAM (NVIDIA GPU).
  # Start with: docker compose --profile gpu up fashn
  # First run downloads ~2 GB of weights; subsequent starts use the cache volume.
  fashn:
    profiles: ["gpu"]
    networks:
      - virtual_closet
    build:
      context: ./docker/fashn
      dockerfile: Dockerfile
    # Reads FASHN_STEPS, FASHN_GUIDANCE, FASHN_GARMENT_PHOTO_TYPE from backend/.env
    env_file:
      - ./backend/.env
    ports:
      - "8002:8000"
    volumes:
      - fashn_weights:/app/weights
      - fashn_cache:/app/model_cache
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 180s

  # TryOff: base gated FLUX.2-klein-base-9B + LoRA fal/virtual-tryoff-lora — ver docker/flux/README.md
  # Start: docker compose --profile tryoff up -d tryoff-model  (or --profile gpu)
  tryoff-model:
    profiles: ["gpu", "tryoff"]
    networks:
      - virtual_closet
    build:
      context: ./docker/flux
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    ports:
      - "8003:8000"
    environment:
      # Dev: menos VRAM/RAM (exit 137 = OOM killer; ver docker/flux/README.md)
      TRYOFF_LOW_VRAM: "true"
      TRYOFF_OFFLOAD: "sequential"
      TRYOFF_STEPS: "12"
      TRYOFF_HEIGHT: "512"
      TRYOFF_WIDTH: "512"
      PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"
    shm_size: "2gb"
    volumes:
      - tryoff_weights:/app/weights
      - tryoff_cache:/app/model_cache
      - ./docker/flux/main.py:/app/main.py:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 300s
    # 137 = SIGKILL (OOM del host). Evita bucle infinito de recargas del modelo (~18 GB).
    restart: on-failure:3

networks:
  virtual_closet:
    name: virtual_closet
    driver: bridge

volumes:
  postgres_data:
  minio_data:
  catvton_cache:
  fashn_weights:
  fashn_cache:
  tryoff_weights:
  tryoff_cache:
